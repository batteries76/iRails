<%= form_with(model: track, url: '/tracks/add_existing', local: true) do |form| %>
  <% if track.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(track.errors.count, "error") %> prohibited this track from being saved:</h2>

      <ul>
      <% track.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= select_tag(:track_id, options_for_select(options)) %>

  <div class="field">
    <%= form.hidden_field :album_id, :value => album.id %>
  </div>

  <div class="field">
    <%= form.label :track_image %>
    <%= form.file_field :track_image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
